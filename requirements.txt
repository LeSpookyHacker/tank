# Core web stack
# Versions pinned to what is tested in this environment.
# Use `pip install -r requirements.txt` to reproduce exactly.
# Upper bounds prevent silent major-version upgrades; review and bump deliberately.
anthropic>=0.92.0,<2.0.0
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
python-multipart>=0.0.12,<1.0.0
python-dotenv>=1.0.1,<2.0.0
pydantic>=2.9.0,<3.0.0
jinja2>=3.1.4,<4.0.0
sse-starlette>=2.1.3,<4.0.0

# Document parsing
pypdf>=5.0.0,<7.0.0
python-docx>=1.1.2,<2.0.0
markdown-it-py>=4.2.0,<5.0.0
pathspec>=0.12.1,<2.0.0
Pillow>=10.0.0,<12.0.0    # image fixture generation (scripts/_gen_fixtures) + vision parser

# Chunking + tokenization
langchain-text-splitters>=0.3.0,<2.0.0
tiktoken>=0.13.0,<1.0.0

# Embeddings (local, no remote API)
sentence-transformers>=3.0.0,<6.0.0
sqlite-vec>=0.1.6,<1.0.0

# Redaction
detect-secrets>=1.5.0,<2.0.0

# Rate limiting
slowapi>=0.1.9,<1.0.0

# Testing
pytest>=8.3.0,<10.0.0

# ---- optional extras (not auto-installed) ----
# Person-name redaction (off by default). To enable:
#   pip install 'spacy>=3.7,<3.8'
#   python -m spacy download en_core_web_sm
#   export TANK_ENABLE_PERSON_REDACTION=1
# Requires Python 3.10+ on recent spaCy versions.
